torch>=2.2
# Qwen3.5 (model_type=qwen3_5) 需 transformers 4.57.0.dev0+；服务器实测 5.10.0.dev0 可加载。
# PyPI 稳定版可能不够，如加载失败从 GitHub 装：pip install git+https://github.com/huggingface/transformers.git
# 注意 5.x 与 4.57 跨大版本，留意 AutoConfig/from_config/Auto 类命名的 API 变动。
transformers>=4.57.0
datasets>=2.18
sacrebleu>=2.4
pydantic>=2.6
PyYAML>=6.0
pytest>=8.0
# Phase 2 蒸馏：accelerate(device_map/量化加载必需) + bitsandbytes(4bit教师/8bit优化器)
accelerate>=0.30
bitsandbytes>=0.43
